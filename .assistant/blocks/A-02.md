# Block: A-02 — Repo awareness + allowed files

> Detail file for this block. Created by `init-block A-02`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Derive allowed file scope from the repo, so tasks are bounded by real paths.

## Definition of Done

Tasks include allowed_files derived from repo structure, and scope validation uses those lists reliably.

---

## Tasks

| ID       | Task                                   | Status  | Done When |
|----------|----------------------------------------|---------|----------|
| A-02-T1  | Repo scan + path index                 | done    | Repo index is built once and accessible by engine |
| A-02-T2  | Allowed files inference per task       | done    | Tasks get allowed_files populated from repo index |
| A-02-T3  | Scope validation uses derived allowlist| done    | validate_scope compares against derived allowed_files |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                      |
|-----------|----------------------------|
| Task ID   | A-02-T3                    |
| Title     | Scope validation uses derived allowlist |
| Status    | done                       |
| Done When | validate_scope compares against derived allowed_files |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-02-T2 — Allowed files inference per task

**Files to modify:**
- `src/engine.ts` — derive allowed_files on initialize and get_next_step

**Files to create:**
- `src/repo/allowlist.ts` — heuristics for allowed files from repo index

**Files NOT touched:**
- `src/tools.ts`, `src/storage/*`

**Approach:**
Use a simple keyword heuristic to map tasks to likely files from the repo index, fallback to empty if no match.

**Risks:**
Low. Heuristic only.

---

### A-02-T3 — Scope validation uses derived allowlist

**Files to modify:**
- `src/engine.ts` — use stored allowlist when none provided
- `src/tools.ts` — allow validate_scope without allowlist
- `mcp-tools-spec.md` — document fallback behavior

**Files to create:**
- none

**Files NOT touched:**
- `src/storage/*`

**Approach:**
Fallback to task allowlist when validate_scope is called without explicit allowed_files. If none exists, require manual approval.

**Risks:**
Low. Only affects validation fallback.

### A-02-T1 — Repo scan + path index

**Files to modify:**
- `src/engine.ts` — store repo index in memory
- `src/index.ts` — build index at startup

**Files to create:**
- `src/repo/indexer.ts` — repo scanner with exclude list

**Files NOT touched:**
- `src/tools.ts`, `src/storage/*`

**Approach:**
Build a simple index of repo files at server startup and expose it via the engine.

**Risks:**
Low. Read-only.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- <!-- note -->

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID  | Status | Note |
|------------|----------|--------|------|
| 2026-04-04 | A-02-T1  | done   | Built repo index and stored in engine |
| 2026-04-04 | A-02-T2  | done   | Derived allowed_files from repo index |
| 2026-04-04 | A-02-T3  | done   | validate_scope uses derived allowlist |

---

_Last updated: 2026-04-04_
