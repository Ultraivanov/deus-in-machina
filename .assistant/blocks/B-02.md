# Block: B-02 — Thin companion UI (optional)

> Detail file for this block. Created by `init-block B-02`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Ship a minimal companion UI that surfaces current state, next step, and session status.

## Definition of Done

A minimal UI exists to view project state and the next step, without blocking MCP usage.

---

## Tasks

| ID       | Task                               | Status  | Done When |
|----------|------------------------------------|---------|----------|
| B-02-T1  | UI scaffold + routing              | done    | Basic UI shell loads |
| B-02-T2  | Project state view                 | done    | UI renders current phase/block/task |
| B-02-T3  | Next step + action panel           | done    | UI displays next step and session status |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                      |
|-----------|----------------------------|
| Task ID   | B-02-T3                    |
| Title     | Next step + action panel |
| Status    | done                       |
| Done When | UI displays next step and session status |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### B-02-T1 — UI scaffold + routing

**Files to create:**
- `ui/index.html` — basic shell
- `ui/styles.css` — minimal visual styling
- `ui/app.js` — render logic
- `ui/router.js` — hash routing

**Files to modify:**
- none

**Files NOT touched:**
- `src/*`

**Approach:**
Static UI shell with hash routing and two views: State and Next Step.

**Risks:**
Low. Static only.

---

### B-02-T2 — Project state view

**Files to modify:**
- `ui/router.js` — render project state from mock data
- `ui/styles.css` — add state list styling

**Files to create:**
- `ui/state.js` — mock state data

**Files NOT touched:**
- `src/*`

**Approach:**
Render phase/block/task and progress info in the State view using mock data.

**Risks:**
Low. Static only.

---

### B-02-T3 — Next step + action panel

**Files to modify:**
- `ui/router.js` — render next step panel
- `ui/state.js` — add next-step mock data
- `ui/styles.css` — action panel styles

**Files to create:**
- none

**Files NOT touched:**
- `src/*`

**Approach:**
Display the next step with why/expected result and a scope list + action panel.

**Risks:**
Low. Static only.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- <!-- note -->

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID  | Status | Note |
|------------|----------|--------|------|
| 2026-04-04 | B-02-T1  | done   | Created UI scaffold with routing |
| 2026-04-04 | B-02-T2  | done   | Added project state view |
| 2026-04-04 | B-02-T3  | done   | Added next step panel |

---

_Last updated: 2026-04-04_
