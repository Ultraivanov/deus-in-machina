# Block: A-01 — SQLite mirror + persistence

> Detail file for this block. Created by `init-block A-01`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Persist the workflow state in SQLite so sessions can resume and monetization can read real usage.

## Definition of Done

Core tools read/write SQLite mirror (projects, tasks, sessions, subscription usage) and remain file-first.

---

## Tasks

| ID       | Task                               | Status  | Done When |
|----------|------------------------------------|---------|----------|
| A-01-T1  | Wire SQLite store into engine      | done    | Engine writes project/phase/block/task/session to SQLite after file writes |
| A-01-T2  | Subscription + usage counters      | pending | Sessions and projects increment usage counters in SQLite |
| A-01-T3  | Resume from SQLite                 | pending | Server can rebuild in-memory state from SQLite when starting |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                      |
|-----------|----------------------------|
| Task ID   | A-01-T2                    |
| Title     | Subscription + usage counters |
| Status    | pending                    |
| Done When | Sessions and projects increment usage counters in SQLite |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-01-T1 — Wire SQLite store into engine

**Files to modify:**
- `src/engine.ts` — write project/phase/block/task/session into SQLite mirror
- `src/index.ts` — initialize SQLite store and pass into engine
- `src/tools.ts` — inject engine via router

**Files to create:**
- `src/storage/index.ts` — SQLite initialization helper

**Files NOT touched:**
- `src/storage/schema.sql`

**Approach:**
Initialize SQLite on server start, inject into the engine, and mirror state changes after file-first writes.

**Risks:**
Medium. SQLite is best-effort and must not break the main loop.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- <!-- note -->

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID  | Status | Note |
|------------|----------|--------|------|
| 2026-04-04 | A-01-T1  | done   | Wired SQLite store into engine + server init |

---

_Last updated: 2026-04-04_
