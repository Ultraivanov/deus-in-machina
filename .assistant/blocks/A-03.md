# Block: A-03 — Error handling + telemetry

> Detail file for this block. Created by `init-block A-03`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Make failures observable and predictable without crashing the core loop.

## Definition of Done

Errors return structured responses, key events are logged, and telemetry hooks are in place.

---

## Tasks

| ID       | Task                               | Status  | Done When |
|----------|------------------------------------|---------|----------|
| A-03-T1  | Structured error envelope          | done    | All tools return consistent error shapes |
| A-03-T2  | Telemetry event hooks              | done    | Key events emit telemetry records |
| A-03-T3  | Error logging + sampling           | pending | Errors are logged with minimal noise |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                      |
|-----------|----------------------------|
| Task ID   | A-03-T3                    |
| Title     | Error logging + sampling |
| Status    | pending                    |
| Done When | Errors are logged with minimal noise |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-03-T2 — Telemetry event hooks

**Files to modify:**
- `src/tools.ts` — emit telemetry on tool events
- `src/index.ts` — set telemetry sink

**Files to create:**
- `src/telemetry.ts` — telemetry helper

**Files NOT touched:**
- `src/storage/*`, `src/engine.ts`

**Approach:**
Add a lightweight telemetry helper and emit best-effort events on each tool call.

**Risks:**
Low. Logging only.

### A-03-T1 — Structured error envelope

**Files to modify:**
- `src/tools.ts` — unify error responses
- `src/index.ts` — enforce error shape on tool responses
- `mcp-tools-spec.md` — document strict error envelope

**Files to create:**
- `src/errors.ts` — shared error helper

**Files NOT touched:**
- `src/storage/*`, `src/engine.ts`

**Approach:**
Introduce a single error helper and ensure all tool handlers return the same structured error shape.

**Risks:**
Low. Formatting only.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- <!-- note -->

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID  | Status | Note |
|------------|----------|--------|------|
| 2026-04-04 | A-03-T1  | done   | Standardized error envelope across tools |
| 2026-04-04 | A-03-T2  | done   | Added telemetry hooks for tool calls |

---

_Last updated: 2026-04-04_
