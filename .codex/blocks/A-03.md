# Block: A-03 — Ruleset tuning + configuration

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Add project-configurable validation rules so DSR can tune runtime behavior on real fixtures without code changes.

## Definition of Done

A shared ruleset config is loaded and applied by validator and fix loop, and deterministic fixture runs prove that a non-default project ruleset changes validation behavior as expected.

---

## Tasks

| ID       | Task                                      | Status    | Done When                                                                  |
|----------|-------------------------------------------|-----------|----------------------------------------------------------------------------|
| A-03-T1  | Ruleset config contract + defaults        | completed | Shared rules loader returns normalized rules with defaults from config input |
| A-03-T2  | Apply configured rules to validator loop  | completed | `validateUI` and `runFixLoop` honor configured thresholds, toggles, and severities |
| A-03-T3  | Fixture vectors for tuned rulesets        | completed | Deterministic strict/relaxed ruleset fixtures produce expected validation outputs |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                |
|-----------|--------------------------------------|
| Task ID   | —                                    |
| Title     | —                                    |
| Status    | —                                    |
| Done When | —                                    |

> All tasks in this block are complete. Block A-03 is done.

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-03-T1 Change Plan (Approved & Executed)

Created ruleset module inspired by Relationship project's token system:
- `src/ruleset/profiles/{default,strict,relaxed}.json` — profile configurations
- `src/ruleset/contract.ts` — TypeScript interfaces
- `src/ruleset/defaults.ts` — normalization logic with defaults
- `src/ruleset/loader.ts` — async loader with caching
- `src/ruleset/index.ts` — module exports

### A-03-T2 Change Plan (Approved & Executed)

Updated validator and fix-loop to use ruleset configuration:
- `validator.js` — async function, imports ruleset, applies dynamic severity
- `fix-loop.js` — async function, passes ruleset to validator

### A-03-T3 Change Plan (Approved & Executed)

Created test fixtures:
- `tests/ruleset.test.js` — comprehensive tests for loader, validation behavior, fix loop

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note |
|------------|------------|-------------|------|
| 2026-04-19 | A-03-T1    | completed   | Created ruleset module with profiles, loader, defaults |
| 2026-04-19 | A-03-T2    | completed   | Updated validator.js and fix-loop.js to use ruleset config |
| 2026-04-19 | A-03-T3    | completed   | Created tests/ruleset.test.js with fixture vectors |

---

_Last updated: 2026-04-19_
