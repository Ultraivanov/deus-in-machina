# Block: PS-03 — Async workflow refactor for runners and CLI

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Repair async handling across validator, fix-loop, and pipeline runners so release smoke passes for runtime vectors and generated artifacts.

## Definition of Done

Validator vectors, fix-loop vectors, and pipeline snapshot comparison all pass with awaited async results, and release smoke fails only on issues outside `PS-03` scope.

---

## Tasks

| ID        | Task                                      | Status  | Done When |
|-----------|-------------------------------------------|---------|-----------|
| PS-03-T1  | Refactor validator vector runner          | pending | `scripts/run-validator-vectors.js` awaits `validateUI()` and validator vectors pass |
| PS-03-T2  | Refactor fix-loop vector runner           | pending | `scripts/run-loop-vectors.js` awaits `runFixLoop()` and fix-loop vectors pass |
| PS-03-T3  | Repair pipeline async artifact generation | pending | `scripts/run-e2e-pipeline.js` writes resolved `validation.json` and `fix-loop.json`, and `scripts/compare-e2e-pipeline.js` passes |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                             |
|-----------|-----------------------------------|
| Task ID   | PS-03-T1                          |
| Title     | Refactor validator vector runner  |
| Status    | pending                           |
| Done When | `scripts/run-validator-vectors.js` awaits `validateUI()` and validator vectors pass |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID   | Status | Note |
|------------|-----------|--------|------|

---

_Last updated: 2026-04-30_
