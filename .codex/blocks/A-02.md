# Block: A-02 — End-to-end CLI pipeline on real project

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Run the full CLI pipeline end-to-end on a real project fixture and capture deterministic outputs.

## Definition of Done

`extract → normalize → patterns → validate → fix → loop` runs on a real fixture and produces stable outputs with a documented run script.

---

## Tasks

| ID       | Task                                         | Status  | Done When                                                     |
|----------|----------------------------------------------|---------|---------------------------------------------------------------|
| A-02-T1  | Build end-to-end CLI runner for real fixture | done    | Script runs full pipeline and outputs artifacts               |
| A-02-T2  | Store pipeline outputs + expected snapshots  | pending | Expected outputs match actual deterministically               |
| A-02-T3  | Document end-to-end run instructions         | pending | README/Docs include a reproducible command sequence           |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | A-02-T2                                    |
| Title     | Store pipeline outputs + expected snapshots |
| Status    | pending                                    |
| Done When | Expected outputs match actual deterministically |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-02-T1 — Build end-to-end CLI runner for real fixture

**Files to modify:**
- `.codex/blocks/A-02.md` — mark task in-progress and log plan

**Files to create:**
- `scripts/run-e2e-pipeline.js` — end-to-end runner
- `examples/fixtures/pipeline-run/.gitkeep`

**Files NOT touched:**
- normalizer, patterns, validator, fix loop, CLI, storage, monetization

**Approach:**
Build a script that reads the real export fixture, derives context, runs
normalizer/patterns/validator/fix loop, and writes artifacts to
`examples/fixtures/pipeline-run/`.

**Risks:**
Script output may evolve as heuristics change; snapshots will need updates.

### <Task ID> — <Task Title>

**Files to modify:**
- `path/to/file` — reason

**Files to create:**
- `path/to/file` — reason

**Files NOT touched:**
- everything else in scope

**Approach:**
<!-- One paragraph. What, how, why this approach. -->

**Risks:**
<!-- Side effects, dependencies, things that could break. -->

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|
| 2026-04-09 | A-02-T1    | done        | Added end-to-end pipeline runner and outputs |

---

_Last updated: 2026-04-09_
