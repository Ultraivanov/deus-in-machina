# Block: I-06 — Fix loop + pipeline integration

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Integrate validation + fix loop so pipeline can iterate until errors are resolved (or max iterations reached).

## Definition of Done

Running loop on fixtures produces a final report with zero ERRORs or stops at max iterations with deterministic outputs.

---

## Tasks

| ID       | Task                                  | Status  | Done When                                                  |
|----------|---------------------------------------|---------|------------------------------------------------------------|
| I-06-T1  | Fix loop core + IO contract            | done    | `runFixLoop` returns `{ final_code, iterations, report }`  |
| I-06-T2  | Integrate pipeline (validate + fix)    | pending | Loop calls `validateUI` → `fixUI` deterministically        |
| I-06-T3  | Fixture/test vectors for loop          | pending | At least 2 loop vectors pass deterministically             |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | I-06-T2                                    |
| Title     | Integrate pipeline (validate + fix)        |
| Status    | in-progress                                |
| Done When | Loop calls `validateUI` → `fixUI` deterministically |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### I-06-T2 — Integrate pipeline (validate + fix)

**Files to modify:**
- `src/fix-loop.js` — add validation + fix loop

**Files to create:**
- `src/fix.js` — deterministic `fixUI`

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, patterns, monetization, stripe, storage

**Approach:**
Implement `fixUI({ code, errors })` with deterministic string corrections. Update
`runFixLoop` to iterate `validateUI` → `fixUI` until errors clear or max iterations.
Return the final report from the last validation.

**Risks:**
String-based fixes are simplistic; may need HTML parsing later.

### I-06-T1 — Fix loop core + IO contract

**Files to modify:**
- none

**Files to create:**
- `src/fix-loop.js` — base `runFixLoop` contract

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, patterns, validator, monetization, stripe, storage

**Approach:**
Create `runFixLoop({ code, rules, maxIterations } = {})` that returns a stable
contract `{ final_code, iterations, report }` with a no-op loop for now. This
establishes the IO baseline for integration in the next task.

**Risks:**
No fix logic yet; expected for the core contract step.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|
| 2026-04-09 | I-06-T1    | done        | Implemented fix loop IO contract |

---

_Last updated: 2026-04-08_
