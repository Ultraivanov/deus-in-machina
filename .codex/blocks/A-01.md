# Block: A-01 — Real Figma export ingestion

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Ingest real Figma export data and map it into DSR context (`variables`, `styles`, `components`, `layout`) deterministically.

## Definition of Done

At least one real Figma export fixture is parsed into DSR context and matches expected output deterministically.

---

## Tasks

| ID       | Task                                           | Status  | Done When                                                     |
|----------|------------------------------------------------|---------|---------------------------------------------------------------|
| A-01-T1  | Define export ingestion contract + parser stub | pending | Parser returns context shape from a real export fixture       |
| A-01-T2  | Map variables/styles/components/layout         | pending | Mapping produces deterministic context for the fixture        |
| A-01-T3  | Fixture + runner for real export ingestion     | pending | Fixture run passes with expected context output              |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | A-01-T1                                    |
| Title     | Define export ingestion contract + parser stub |
| Status    | pending                                    |
| Done When | Parser returns context shape from a real export fixture |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### <Task ID> — <Task Title>

**Files to modify:**
- `path/to/file` — reason

**Files to create:**
- `path/to/file` — reason

**Files NOT touched:**
- everything else in scope

**Approach:**
<!-- One paragraph. What, how, why this approach. -->

**Risks:**
<!-- Side effects, dependencies, things that could break. -->

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|

---

_Last updated: 2026-04-09_
