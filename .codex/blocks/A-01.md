# Block: A-01 — Real Figma export ingestion

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Ingest real Figma export data and map it into DSR context (`variables`, `styles`, `components`, `layout`) deterministically.

## Definition of Done

At least one real Figma export fixture is parsed into DSR context and matches expected output deterministically.

---

## Tasks

| ID       | Task                                           | Status  | Done When                                                     |
|----------|------------------------------------------------|---------|---------------------------------------------------------------|
| A-01-T1  | Define export ingestion contract + parser stub | done    | Parser returns context shape from a real export fixture       |
| A-01-T2  | Map variables/styles/components/layout         | done    | Mapping produces deterministic context for the fixture        |
| A-01-T3  | Fixture + runner for real export ingestion     | done    | Fixture run passes with expected context output              |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | -                                          |
| Title     | -                                          |
| Status    | done                                       |
| Done When | -                                          |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### A-01-T2 — Map variables/styles/components/layout

**Files to modify:**
- `src/adapters/figma-export.js` — add mapping for variables/styles/components/layout

**Files to create:**
- none

**Files NOT touched:**
- normalizer, patterns, validator, fix loop, CLI, storage, monetization

**Approach:**
Parse `data-node-id` + `data-name` pairs into layout entries, derive component
stubs from instance-like names, collect color literals into variables, and extract
basic typography tokens from className fragments.

**Risks:**
Heuristics are string-based and may require updates if export format changes.

---

### A-01-T3 — Fixture + runner for real export ingestion

**Files to modify:**
- `.codex/blocks/A-01.md` — mark task in-progress and log plan

**Files to create:**
- `examples/fixtures/figma-export/section-form.expected.json`
- `examples/fixtures/figma-export/section-form.actual.json`
- `scripts/run-figma-export-fixture.js`

**Files NOT touched:**
- normalizer, patterns, validator, fix loop, CLI, storage, monetization

**Approach:**
Generate expected output from `parseFigmaExport`, store it as a fixture, and add a
runner script to recompute actual output and compare deterministically.

**Risks:**
Mapping changes will require updates to expected outputs.

### A-01-T1 — Define export ingestion contract + parser stub

**Files to modify:**
- `.codex/blocks/A-01.md` — mark task in-progress and log plan

**Files to create:**
- `src/adapters/figma-export.js` — MCP export parser stub
- `examples/fixtures/figma-export/section-form.code.txt` — raw MCP export
- `examples/fixtures/figma-export/section-form.input.json` — input wrapper

**Files NOT touched:**
- normalizer, patterns, validator, fix loop, CLI, storage, monetization

**Approach:**
Persist the MCP export as fixture input and add a parser stub that extracts node
IDs from `data-node-id` and returns a valid `{ context, meta }` shape.

**Risks:**
Large fixture file size; manageable for Alpha phase validation.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|
| 2026-04-09 | A-01-T1    | done        | Added MCP export fixture + parser stub |
| 2026-04-09 | A-01-T2    | done        | Added mapping for variables/styles/components/layout |
| 2026-04-09 | A-01-T3    | done        | Added fixture runner for real export ingestion |

---

_Last updated: 2026-04-09_
