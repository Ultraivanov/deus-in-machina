# Block: I-02 — Extractor adapter (Figma input)

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Implement an adapter that converts Figma data into the extractor context format.

## Definition of Done

Adapter accepts a Figma file key and outputs `context.json` per schema.

---

## Tasks

| ID       | Task                             | Status  | Done When                                         |
|----------|----------------------------------|---------|---------------------------------------------------|
| I-02-T1  | Adapter interface + input parsing | done    | Input parsing documented and stub implemented     |
| I-02-T2  | Output mapping to context schema  | done    | Output mapping implemented                         |
| I-02-T3  | Basic test fixture                | done    | At least one fixture run produces context JSON    |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                          |
|-----------|--------------------------------|
| Task ID   | —                              |
| Title     | Block complete                 |
| Status    | done                           |
| Done When | All block tasks completed and DoD met |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### I-02-T1 — Adapter interface + input parsing

**Files to modify:**
- `src/adapters/figma.js` — add stub adapter and input parsing

**Files to create:**
- `src/adapters/figma.js` — adapter entrypoint

**Files NOT touched:**
- everything else in scope

**Approach:**
Implement a stub adapter that accepts `fileKey` and optional `nodeIds` and returns an empty context shell.

**Risks:**
Real Figma API integration may require auth and rate limit handling.

### I-02-T2 — Output mapping to context schema

**Files to modify:**
- `src/adapters/figma.js` — map adapter output to context schema

**Files to create:**
- none

**Files NOT touched:**
- everything else in scope

**Approach:**
Populate `variables`, `styles`, `components`, and `layout` from adapter input (stub for now).

**Risks:**
Mapping may need adjustments after real Figma data.

### I-02-T3 — Basic test fixture

**Files to modify:**
- `src/adapters/figma.js` — keep export stable for fixture usage (only if needed)

**Files to create:**
- `examples/fixtures/figma-input.json` — minimal raw fixture input
- `examples/fixtures/figma-context.expected.json` — expected mapped context output
- `scripts/run-fixture.js` — simple runner that executes adapter and writes output

**Files NOT touched:**
- Runtime/engine/mcp modules outside adapter fixture flow

**Approach:**
Create a deterministic fixture input, run `extractFigmaContext`, and verify produced JSON shape against expected output.

**Risks:**
Fixture may drift when adapter schema evolves; keep expected file explicit and versioned.

---

## Refactor Backlog

- none

---

## Session Log

| Date       | Task ID   | Status  | Note |
|------------|-----------|---------|------|
| 2026-04-05 | I-02-T1   | done    | Adapter stub created |
| 2026-04-07 | I-02-T2   | done    | Output mapping implemented for context schema |
| 2026-04-07 | I-02-T3   | done    | Fixture run produced expected context JSON |

---

_Last updated: 2026-04-05_
