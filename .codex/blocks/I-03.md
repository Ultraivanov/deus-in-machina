# Block: I-03 — Normalizer implementation

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Implement a working normalizer that converts raw Figma variables/styles into deterministic semantic tokens.

## Definition of Done

Normalizer run on fixture inputs returns `normalized_context.tokens` in `category.role[.scale][.state]` format and base test vectors pass.

---

## Tasks

| ID       | Task                                      | Status  | Done When                                                   |
|----------|-------------------------------------------|---------|-------------------------------------------------------------|
| I-03-T1  | Core normalization pipeline               | done    | Pipeline returns `normalized_context.tokens` and `meta`     |
| I-03-T2  | Deterministic mapping + duplicate resolution | done    | Mapping rules and collision resolution are implemented      |
| I-03-T3  | Fixture/test vectors for normalizer       | done    | At least three vectors produce expected normalized output   |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | -                                          |
| Title     | -                                          |
| Status    | done                                       |
| Done When | -                                          |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### I-03-T1 — Core normalization pipeline

**Files to modify:**
- `src/normalizer.js` — implement normalization pipeline

**Files to create:**
- `src/normalizer.js` — normalizer module (if missing)
- `examples/fixtures/normalizer-input.json` — minimal fixture input
- `examples/fixtures/normalizer-output.expected.json` — expected normalized output

**Files NOT touched:**
- engine/tool routing, billing, stripe, and unrelated adapter modules

**Approach:**
Build a deterministic normalizer for `variables` and `styles` that emits `normalized_context.tokens` and count metadata. Keep implementation strict and predictable before adding richer mapping heuristics.

**Risks:**
Raw naming diversity and token collisions may require additional logic in the next task.

### I-03-T2 — Deterministic mapping + duplicate resolution

**Files to modify:**
- `src/normalizer.js` — add strict mapping heuristics and deterministic collision policy
- `examples/fixtures/normalizer-input.json` — extend fixture with collision cases
- `examples/fixtures/normalizer-output.expected.json` — expected output with deterministic winner

**Files to create:**
- none

**Files NOT touched:**
- workflow engine, MCP tools router, monetization, stripe modules

**Approach:**
Implement explicit mapping rules for category/role/scale/state and deterministic duplicate resolution order so equal semantic tokens always resolve the same way.

**Risks:**
Rule strictness may reduce recall on uncommon naming conventions; can be relaxed in later tasks with explicit fallback policy.

### I-03-T3 — Fixture/test vectors for normalizer

**Files to modify:**
- `examples/fixtures/normalizer-input.json` — split into vectorized fixtures
- `examples/fixtures/normalizer-output.expected.json` — align with vector outputs

**Files to create:**
- `examples/fixtures/normalizer-vectors/primary-color.input.json`
- `examples/fixtures/normalizer-vectors/primary-color.expected.json`
- `examples/fixtures/normalizer-vectors/neutral-scale.input.json`
- `examples/fixtures/normalizer-vectors/neutral-scale.expected.json`
- `examples/fixtures/normalizer-vectors/spacing.input.json`
- `examples/fixtures/normalizer-vectors/spacing.expected.json`
- `scripts/run-normalizer-vectors.js` — run all vectors and fail on mismatch

**Files NOT touched:**
- adapter, workflow engine, MCP router

**Approach:**
Create three explicit vectors from architecture examples and run them through one deterministic test runner to verify token shape and stable mapping behavior.

**Risks:**
Fixture maintenance overhead when mapping rules evolve; offset by clear vector naming and small inputs.

---

## Refactor Backlog

- none

---

## Session Log

| Date       | Task ID   | Status  | Note |
|------------|-----------|---------|------|
| 2026-04-07 | I-03-T1   | pending | Block initialized and task selected |
| 2026-04-07 | I-03-T1   | done    | Core normalization pipeline and fixture baseline implemented |
| 2026-04-07 | I-03-T2   | done    | Deterministic mapping and collision resolution added |
| 2026-04-08 | I-03-T3   | done    | Added vector coverage and verified outputs |

---

_Last updated: 2026-04-07_
