# Block: I-05 — Validator implementation

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Implement a deterministic validator that checks UI artifacts against rules and returns structured error/warning/info reports.

## Definition of Done

Validator run on fixture inputs returns expected `valid`, `summary`, and rule IDs for at least three vectors.

---

## Tasks

| ID       | Task                                   | Status  | Done When                                                   |
|----------|----------------------------------------|---------|-------------------------------------------------------------|
| I-05-T1  | Validator core + IO contract            | pending | `validateUI` returns `{ valid, summary, errors, warnings, infos }` |
| I-05-T2  | Rule checks (token + spacing + pattern) | pending | Deterministic checks emit expected rule IDs and severities  |
| I-05-T3  | Fixture/test vectors for validator      | pending | Three vectors pass with deterministic outputs               |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | I-05-T1                                    |
| Title     | Validator core + IO contract               |
| Status    | in-progress                                |
| Done When | `validateUI` returns `{ valid, summary, errors, warnings, infos }` |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### I-05-T1 — Validator core + IO contract

**Files to modify:**
- none

**Files to create:**
- `src/validator.js` — base `validateUI` contract

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, patterns, fixes, monetization, stripe, storage

**Approach:**
Create `validateUI({ code, rules } = {})` that returns a stable contract
`{ valid, summary, errors, warnings, infos }` with empty results by default.
This establishes the IO baseline for rule checks in T2.

**Risks:**
No actual validation logic yet; expected for the core contract step.

---

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|

---

_Last updated: 2026-04-08_
