# Block: P1-01 — Token model + mapping rules

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Define the token model and deterministic mapping rules from raw Figma data to semantic tokens.

## Definition of Done

A written spec exists for raw/normalized/semantic token layers, naming conventions, and duplicate resolution, with concrete examples.

---

## Tasks

| ID       | Task                                   | Status  | Done When                                                        |
|----------|----------------------------------------|---------|------------------------------------------------------------------|
| P1-01-T1 | Token model schema + naming conventions | done    | Schema and naming rules documented with examples                 |
| P1-01-T2 | Mapping rules + duplicate resolution    | done    | Rules documented and edge cases described                        |
| P1-01-T3 | Example inputs/outputs for normalization | done    | At least 3 complete input→output examples captured               |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                   |
|-----------|-----------------------------------------|
| Task ID   | P1-01-T3                                |
| Title     | Example inputs/outputs for normalization |
| Status    | done                                    |
| Done When | At least 3 complete input→output examples captured |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### P1-01-T1 — Token model schema + naming conventions

**Files to modify:**
- `.codex/ARCHITECTURE.md` — formalize token schema section
- `.codex/SNAPSHOT.md` — align summary if needed

**Files to create:**
- none

**Files NOT touched:**
- everything else in scope

**Approach:**
Document the three-layer token model (raw/normalized/semantic), define naming conventions, and add 2–3 examples that map raw names to semantic tokens.

**Risks:**
Overfitting to current naming patterns; may require iteration once real Figma data arrives.

### P1-01-T2 — Mapping rules + duplicate resolution

**Files to modify:**
- `.codex/ARCHITECTURE.md` — extend mapping rules, dedup, and edge cases

**Files to create:**
- none

**Files NOT touched:**
- everything else in scope

**Approach:**
Define deterministic mapping steps, explicit dedup precedence, and common edge-case handling for normalization.

**Risks:**
Ambiguous naming in real data may require overrides or configurable rules.

### P1-01-T3 — Example inputs/outputs for normalization

**Files to modify:**
- `.codex/ARCHITECTURE.md` — add full raw → normalized → semantic examples

**Files to create:**
- none

**Files NOT touched:**
- everything else in scope

**Approach:**
Capture at least three concrete examples across color, spacing, and typography to make the mapping unambiguous.

**Risks:**
Examples may need expansion when real token types (e.g., shadows) arrive.

---

## Refactor Backlog

- none

---

## Session Log

| Date       | Task ID   | Status      | Note |
|------------|-----------|-------------|------|
| 2026-04-05 | P1-01-T1  | done        | Token model and naming conventions documented |
| 2026-04-05 | P1-01-T2  | done        | Mapping rules, dedup, and edge cases documented |
| 2026-04-05 | P1-01-T3  | done        | Added full normalization examples |

---

_Last updated: 2026-04-05_
