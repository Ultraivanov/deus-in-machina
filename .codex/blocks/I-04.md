# Block: I-04 — Pattern engine implementation

> Detail file for this block. Created by `init-block <ID>`.
> `PHASES.md` tracks block status only. All task detail lives here.

---

## Block Goal

Implement a deterministic pattern engine that detects Hero/Card/List from layout context and returns stable pattern outputs.

## Definition of Done

Running pattern detection on fixture layouts returns expected pattern types/structures and `meta.detectedCount` matches across at least three vectors.

---

## Tasks

| ID       | Task                                      | Status  | Done When                                                   |
|----------|-------------------------------------------|---------|-------------------------------------------------------------|
| I-04-T1  | Pattern engine core + IO contract         | done    | `detectPatterns` returns `{ patterns, meta }` and respects `minConfidence` |
| I-04-T2  | Hero/Card/List heuristics + confidence    | done    | Heuristics produce expected types/structures on sample layouts |
| I-04-T3  | Fixture/test vectors for patterns         | done    | Three vectors pass with deterministic outputs               |

> New tasks are added here as the block progresses via `init-task`.

---

## Active Task

| Field     | Value                                      |
|-----------|--------------------------------------------|
| Task ID   | -                                          |
| Title     | -                                          |
| Status    | done                                       |
| Done When | -                                          |

---

## Change Plans

> One entry per task. Written by agent before coding. Approved by user before execution.

### I-04-T1 — Pattern engine core + IO contract

**Files to modify:**
- none

**Files to create:**
- `src/patterns.js` — base `detectPatterns` module and IO contract

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, validator, fixes, monetization, stripe, storage

**Approach:**
Create `detectPatterns({ context = {}, options = {} } = {})` that normalizes `minConfidence`,
counts layout nodes, and returns `{ patterns, meta }` with a deterministic contract. This
establishes the baseline IO so heuristics can be layered in the next task.

**Risks:**
Initial output is empty until heuristics land; downstream callers should treat it as valid but empty.

---

### I-04-T2 — Hero/Card/List heuristics + confidence

**Files to modify:**
- `src/patterns.js` — add Hero/Card/List heuristics and confidence scoring

**Files to create:**
- none

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, validator, fixes, monetization, stripe, storage

**Approach:**
Add simple detectors over layout context to infer Hero (headline/subheadline/cta),
Card (container + title + description + optional media), and List (repeated item
structures). Each detector produces a deterministic confidence score based on
matched cues, and the final output filters by `minConfidence`.

**Risks:**
Heuristics rely on simplified layout naming and may need expansion for real trees.

---

### I-04-T3 — Fixture/test vectors for patterns

**Files to modify:**
- `.codex/blocks/I-04.md` — mark task in-progress and log plan

**Files to create:**
- `examples/fixtures/pattern-vectors/hero.input.json`
- `examples/fixtures/pattern-vectors/hero.expected.json`
- `examples/fixtures/pattern-vectors/hero.actual.json`
- `examples/fixtures/pattern-vectors/card.input.json`
- `examples/fixtures/pattern-vectors/card.expected.json`
- `examples/fixtures/pattern-vectors/card.actual.json`
- `examples/fixtures/pattern-vectors/list.input.json`
- `examples/fixtures/pattern-vectors/list.expected.json`
- `examples/fixtures/pattern-vectors/list.actual.json`
- `scripts/run-pattern-vectors.js`

**Files NOT touched:**
- CLI, workflow engine, adapters, normalizer, validator, fixes, monetization, stripe, storage

**Approach:**
Create three minimal layout vectors (Hero/Card/List) using `role` fields and run
them through `detectPatterns`. Save expected outputs and implement a vector runner
to write `.actual.json` and fail on mismatches.

**Risks:**
Heuristics and layout formats may evolve, requiring updates to expected outputs.

## Refactor Backlog

> Spotted during this block. Do not touch until a dedicated refactor block.

- none

---

## Session Log

> One line per session. Written by agent on `/fi`.

| Date       | Task ID    | Status      | Note                  |
|------------|------------|-------------|-----------------------|
| 2026-04-08 | I-04-T1    | done        | Implemented base pattern IO contract |
| 2026-04-08 | I-04-T2    | done        | Added Hero/Card/List heuristics and confidence |
| 2026-04-08 | I-04-T3    | done        | Added pattern vectors and runner script |

---

_Last updated: 2026-04-08_
